# Local environment — never commit real credentials. Template is .env.example.
.env
.env.*
!.env.example

# Docker bind-mount data (etcd, minio, milvus, postgres)
volumes/

# Python
__pycache__/
*.pyc
*.py[cod]
*.egg-info/
.eggs/
build/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Node / dashboard
node_modules/
dist/
.vite/
*.tsbuildinfo
*.log

# Ops artefacts — --json-out summaries from ops/milvus_demo.py.
# demo_results.json is what `make demo` writes; results.json is the name used
# in the documented example invocation.
results.json
demo_results.json

# Editors and OS
.DS_Store
.idea/
.vscode/
*.swp
